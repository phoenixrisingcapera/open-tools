---
description: Show the Open Tools command menu and recommend the right agent/command for the task.
agent: toolbox-navigator
---

Help me choose the right Open Tools agent or command.

Current task or number from menu:
$ARGUMENTS

Before routing, preserve these default Open Tools guarantees:

- claim one bounded `IN PROGRESS` session before substantial work,
- avoid overlapping an existing owner claim,
- treat active/idle endpoints plus latent/unmounted features as protected by default,
- preserve established layouts unless the user explicitly requests a bounded layout change.
